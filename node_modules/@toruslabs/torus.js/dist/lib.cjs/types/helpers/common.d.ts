import { JRPCResponse } from "@toruslabs/constants";
import { Ecies } from "@toruslabs/eccrypto";
import { base64ToBytes as mhBase64ToBytes, bigintToHex, bytesToBase64 as mhBytesToBase64, bytesToHex, bytesToNumberBE, bytesToNumberLE, calculateMedian, concatBytes, Curve, derivePubKey, generatePrivateKey, getEd25519, getKeyCurve, getSecp256k1, hexToBytes, invert, kCombinations, keccak256, keccak256Bytes, mod, numberToBytesBE, thresholdSame, toBigIntBE, utf8ToBytes } from "@toruslabs/metadata-helpers";
import { CommitmentRequestResult, EciesHex, GetORSetKeyResponse, VerifierLookupResponse } from "../interfaces";
export { bigintToHex, bytesToHex, bytesToNumberBE, bytesToNumberLE, calculateMedian, concatBytes, derivePubKey, generatePrivateKey, getEd25519, getKeyCurve, getSecp256k1, hexToBytes, invert, kCombinations, keccak256, keccak256Bytes, mod, numberToBytesBE, thresholdSame, toBigIntBE, utf8ToBytes, };
export type { Curve };
export declare const bytesToBase64: typeof mhBytesToBase64;
export declare const base64ToBytes: typeof mhBase64ToBytes;
export declare const normalizeKeysResult: (result: GetORSetKeyResponse) => Pick<GetORSetKeyResponse, "keys" | "is_new_key">;
export declare const normalizeLookUpResult: (result: VerifierLookupResponse) => Pick<VerifierLookupResponse, "keys">;
export declare function encParamsBufToHex(encParams: Ecies): EciesHex;
/** ECIES params: hex → bytes. */
export declare function encParamsHexToBuf(eciesData: Omit<EciesHex, "ciphertext">): Omit<Ecies, "ciphertext">;
export declare function getProxyCoordinatorEndpointIndex(endpoints: string[], verifier: string, verifierId: string): number;
export declare function waitFor(milliseconds: number): Promise<unknown>;
export declare function retryCommitment(executionPromise: () => Promise<JRPCResponse<CommitmentRequestResult>>, maxRetries: number): Promise<JRPCResponse<CommitmentRequestResult>>;
export declare function isNullOrUndefined(value: unknown): value is null | undefined;
