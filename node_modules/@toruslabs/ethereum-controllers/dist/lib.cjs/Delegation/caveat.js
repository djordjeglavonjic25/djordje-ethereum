'use strict';

var _objectSpread = require('@babel/runtime/helpers/objectSpread2');
var delegationCore = require('@metamask/delegation-core');
var viem = require('viem');
var abis = require('../utils/abis.js');
var delegationTypes = require('../utils/delegationTypes.js');
var environment = require('./environment.js');
var execution = require('./execution.js');

const SPECIFIC_ACTION_ERC20_TRANSFER_BATCH_TERMS_MIN_BYTES = 124;
/**
 * Decodes packed terms for {@link SpecificActionERC20TransferBatchEnforcer}.
 *
 * Chain-agnostic: terms layout is identical on every deployment. Prefer this over
 * {@link decodeCaveat} when you already know the enforcer and only need the packed fields.
 *
 * Layout matches `getTermsInfo` in MetaMask delegation-framework:
 * token (20) | recipient (20) | amount (32) | firstTarget (20) | firstValue (32) | firstCalldata (…)
 */
function decodeSpecificActionERC20TransferBatchTerms(terms) {
  const hexBody = terms.startsWith("0x") ? terms.slice(2) : terms;
  if (hexBody.length % 2 !== 0) {
    throw new Error("SpecificActionERC20TransferBatchEnforcer: invalid terms hex length");
  }
  if (hexBody.length < SPECIFIC_ACTION_ERC20_TRANSFER_BATCH_TERMS_MIN_BYTES * 2) {
    throw new Error("SpecificActionERC20TransferBatchEnforcer: invalid terms length");
  }
  const tokenAddress = viem.getAddress(`0x${hexBody.slice(0, 40)}`);
  const recipient = viem.getAddress(`0x${hexBody.slice(40, 80)}`);
  const amount = BigInt(`0x${hexBody.slice(80, 144)}`);
  const firstTarget = viem.getAddress(`0x${hexBody.slice(144, 184)}`);
  const firstValue = BigInt(`0x${hexBody.slice(184, 248)}`);
  const firstCalldataBody = hexBody.slice(248);
  const firstCalldata = firstCalldataBody.length === 0 ? "0x" : `0x${firstCalldataBody}`;
  return {
    tokenAddress,
    recipient,
    amount,
    firstTarget,
    firstValue,
    firstCalldata
  };
}
/**
 * Derives the enforced two-transaction batch from SpecificActionERC20TransferBatch terms.
 *
 * `executions[0]` uses `firstCalldata` verbatim from terms; `executions[1].callData` is
 * re-encoded as standard ERC-20 `transfer(recipient, amount)` (not stored in terms).
 */
function specificActionERC20TransferBatchTermsToExecutions(terms) {
  const transferCalldata = viem.encodeFunctionData({
    abi: abis.erc20Abi,
    functionName: "transfer",
    args: [terms.recipient, terms.amount]
  });
  return [execution.normalizeExecutionStruct({
    target: terms.firstTarget,
    value: terms.firstValue,
    callData: terms.firstCalldata
  }), execution.normalizeExecutionStruct({
    target: terms.tokenAddress,
    value: 0n,
    callData: transferCalldata
  })];
}
/**
 * Resolves a caveat enforcer contract address to its deployment name for the given chain.
 */
function getEnforcerName(enforcer, chainId, options = {}) {
  const normalized = enforcer.toLowerCase();
  for (const name of delegationTypes.SUPPORTED_CAVEAT_ENFORCERS) {
    try {
      const address = environment.getCaveatEnforcerAddress(chainId, name, options);
      if (address.toLowerCase() === normalized) {
        return name;
      }
    } catch {
      // Enforcer not deployed on this chain; try the next supported name.
    }
  }
  return undefined;
}
/**
 * Builds the default caveat set for a gasless delegation: one call limit plus exact execution(s).
 */
function buildDefaultCaveats(chainId, executions, options = {}) {
  if (executions.length === 0) {
    throw new Error("Cannot build caveats without executions");
  }
  const caveats = [{
    enforcer: environment.getCaveatEnforcerAddress(chainId, delegationTypes.CAVEAT_ENFORCER.LimitedCalls, options),
    terms: delegationCore.createLimitedCallsTerms({
      limit: 1
    }),
    args: "0x"
  }];
  if (executions.length > 1) {
    caveats.push({
      enforcer: environment.getCaveatEnforcerAddress(chainId, delegationTypes.CAVEAT_ENFORCER.ExactExecutionBatch, options),
      terms: delegationCore.createExactExecutionBatchTerms({
        executions
      }),
      args: "0x"
    });
  } else {
    const execution = executions[0];
    caveats.push({
      enforcer: environment.getCaveatEnforcerAddress(chainId, delegationTypes.CAVEAT_ENFORCER.ExactExecution, options),
      terms: delegationCore.createExactExecutionTerms({
        execution
      }),
      args: "0x"
    });
  }
  return caveats;
}
/**
 * Decodes a caveat's `terms` into a typed structure when the enforcer is recognized on the chain.
 *
 * `chainId` must match the delegation's chain so {@link getEnforcerName} can resolve the
 * enforcer address from deployments. Unrecognized addresses fall back to `unknown` even when
 * terms are decodable via enforcer-specific helpers (e.g. {@link decodeSpecificActionERC20TransferBatchTerms}).
 */
function decodeCaveat(caveat, chainId, options = {}) {
  const enforcerName = getEnforcerName(caveat.enforcer, chainId, options);
  try {
    if (enforcerName === delegationTypes.CAVEAT_ENFORCER.LimitedCalls) {
      return _objectSpread({
        type: "LimitedCalls"
      }, delegationCore.decodeLimitedCallsTerms(caveat.terms));
    }
    if (enforcerName === delegationTypes.CAVEAT_ENFORCER.ExactExecution) {
      const {
        execution: execution$1
      } = delegationCore.decodeExactExecutionTerms(caveat.terms);
      return {
        type: "ExactExecution",
        execution: execution.normalizeExecutionStruct(execution$1)
      };
    }
    if (enforcerName === delegationTypes.CAVEAT_ENFORCER.ExactExecutionBatch) {
      const {
        executions
      } = delegationCore.decodeExactExecutionBatchTerms(caveat.terms);
      return {
        type: "ExactExecutionBatch",
        executions: executions.map(execution.normalizeExecutionStruct)
      };
    }
    if (enforcerName === delegationTypes.CAVEAT_ENFORCER.SpecificActionERC20TransferBatch) {
      const terms = decodeSpecificActionERC20TransferBatchTerms(caveat.terms);
      return _objectSpread(_objectSpread({
        type: "SpecificActionERC20TransferBatch"
      }, terms), {}, {
        executions: specificActionERC20TransferBatchTermsToExecutions(terms)
      });
    }
  } catch {
    // Fall through to unknown fallback.
  }
  return {
    type: "unknown",
    enforcer: caveat.enforcer,
    terms: caveat.terms,
    args: caveat.args
  };
}

Object.defineProperty(exports, "createExactExecutionBatchTerms", {
    enumerable: true,
    get: function () { return delegationCore.createExactExecutionBatchTerms; }
});
Object.defineProperty(exports, "createExactExecutionTerms", {
    enumerable: true,
    get: function () { return delegationCore.createExactExecutionTerms; }
});
Object.defineProperty(exports, "createLimitedCallsTerms", {
    enumerable: true,
    get: function () { return delegationCore.createLimitedCallsTerms; }
});
Object.defineProperty(exports, "decodeExactExecutionBatchTerms", {
    enumerable: true,
    get: function () { return delegationCore.decodeExactExecutionBatchTerms; }
});
Object.defineProperty(exports, "decodeExactExecutionTerms", {
    enumerable: true,
    get: function () { return delegationCore.decodeExactExecutionTerms; }
});
Object.defineProperty(exports, "decodeLimitedCallsTerms", {
    enumerable: true,
    get: function () { return delegationCore.decodeLimitedCallsTerms; }
});
exports.buildDefaultCaveats = buildDefaultCaveats;
exports.decodeCaveat = decodeCaveat;
exports.decodeSpecificActionERC20TransferBatchTerms = decodeSpecificActionERC20TransferBatchTerms;
exports.getEnforcerName = getEnforcerName;
exports.specificActionERC20TransferBatchTermsToExecutions = specificActionERC20TransferBatchTermsToExecutions;
