'use strict';

var delegationCore = require('@metamask/delegation-core');
var viem = require('viem');
var delegation = require('./delegation.js');
var execution = require('./execution.js');

/**
 * Converts EIP-5792 batch transaction params into delegation {@link ExecutionStruct} entries.
 */
function batchTransactionParamsToExecutions(transactions) {
  return transactions.map(transaction => {
    var _transaction$to, _transaction$value, _transaction$data;
    return {
      target: viem.getAddress((_transaction$to = transaction.to) !== null && _transaction$to !== void 0 ? _transaction$to : viem.zeroAddress),
      value: BigInt((_transaction$value = transaction.value) !== null && _transaction$value !== void 0 ? _transaction$value : "0x0"),
      callData: (_transaction$data = transaction.data) !== null && _transaction$data !== void 0 ? _transaction$data : "0x"
    };
  });
}
/**
 * Converts delegation {@link ExecutionStruct} entries into EIP-5792 batch transaction params.
 */
function executionsToBatchTransactionParams(executions) {
  return executions.map(execution => ({
    to: execution.target,
    value: viem.toHex(execution.value),
    data: execution.callData
  }));
}
/**
 * Decodes `redeemDelegations` calldata into delegations, execution modes, and executions.
 */
function decodeRedeemDelegations(data) {
  const encodedParams = execution.stripRedeemDelegationsSelector(data);
  const [contexts, modes, calldatas] = viem.decodeAbiParameters(viem.parseAbiParameters("bytes[], bytes32[], bytes[]"), encodedParams);
  if (modes.length !== calldatas.length) {
    throw new Error("redeemDelegations modes and calldatas length mismatch");
  }
  if (contexts.length !== modes.length) {
    throw new Error("redeemDelegations contexts and modes length mismatch");
  }
  const delegations = contexts.map(context => delegationCore.decodeDelegations(context).map(delegation.fromDelegationStruct));
  const executions = calldatas.map((calldata, index) => execution.decodeExecutionCalldata(calldata, modes[index]));
  return {
    delegations,
    modes: modes,
    executions
  };
}

exports.batchTransactionParamsToExecutions = batchTransactionParamsToExecutions;
exports.decodeRedeemDelegations = decodeRedeemDelegations;
exports.executionsToBatchTransactionParams = executionsToBatchTransactionParams;
