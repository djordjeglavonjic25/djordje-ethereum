import { ANY_BENEFICIARY, Delegation as CoreDelegationStruct, ROOT_AUTHORITY } from "@metamask/delegation-core";
import { Hex } from "viem";
import { Delegation, DelegationStruct, DelegationTypedData, EncodeRedeemDelegationsRequestCalldataResult, EncodeRedeemDelegationsRequestDelegationResult, EncodeRedeemDelegationsRequestInput, ExecutionMode, ExecutionStruct, PrepareDelegationInput, PrepareDelegationResult, RedeemDelegationsOutput, UnsignedDelegation } from "../utils/delegationTypes";
export { ANY_BENEFICIARY, ROOT_AUTHORITY };
/**
 * Converts a controller {@link Delegation} into the struct shape expected by `@metamask/delegation-core`.
 */
export declare function toDelegationStruct(delegation: Delegation): DelegationStruct;
/**
 * Converts a `@metamask/delegation-core` delegation struct into the controller {@link Delegation} type.
 */
export declare function fromDelegationStruct(delegation: CoreDelegationStruct<Hex>): Delegation;
/**
 * Returns the EIP-712 delegation hash for offchain verification or display.
 */
export declare function getDelegationHashOffchain(delegation: Delegation): Hex;
/**
 * Builds EIP-712 typed data for the delegator to sign an {@link UnsignedDelegation}.
 */
export declare function getDelegationTypedData(delegation: UnsignedDelegation, { chainId, delegationManager }: {
    chainId: number;
    delegationManager: Hex;
}): DelegationTypedData;
/**
 * Generates a random 32-byte salt for a new delegation.
 */
export declare function generateDelegationSalt(): Hex;
/**
 * Prepares an unsigned delegation for gasless redemption (step 1 of the two-step flow).
 *
 * Builds default caveats from the planned executions, derives execution modes, and returns
 * EIP-712 typed data for the delegator to sign. The relay `to` address is the chain's
 * `DelegationManager`.
 */
export declare function prepareDelegation({ delegator, chainId, executions, environmentOptions }: PrepareDelegationInput): PrepareDelegationResult;
/**
 * Encodes nested delegation chains into permission context bytes for `redeemDelegations`.
 */
export declare function encodePermissionContexts(delegations: Delegation[][]): Hex[];
/**
 * Encodes full `redeemDelegations` calldata from delegations, execution modes, and executions.
 */
export declare function encodeRedeemDelegations({ delegations, modes, executions, }: {
    delegations: Delegation[][];
    modes: ExecutionMode[];
    executions: ExecutionStruct[][];
}): Hex;
/**
 * Encodes a signed delegation into a gasless `redeemDelegations` request.
 *
 * Call after {@link prepareDelegation} once the delegator has signed the unsigned delegation.
 * Use {@link RedeemDelegationsOutput.Calldata} to get relay-ready calldata and the
 * `DelegationManager` target (BYOK / Sentinel). Use {@link RedeemDelegationsOutput.Delegation}
 * to return the signed delegation with execution metadata only (no calldata encoding).
 */
export declare function encodeRedeemDelegationsRequest(input: EncodeRedeemDelegationsRequestInput, options: {
    output: RedeemDelegationsOutput.Calldata;
}): EncodeRedeemDelegationsRequestCalldataResult;
export declare function encodeRedeemDelegationsRequest(input: EncodeRedeemDelegationsRequestInput, options: {
    output: RedeemDelegationsOutput.Delegation;
}): EncodeRedeemDelegationsRequestDelegationResult;
