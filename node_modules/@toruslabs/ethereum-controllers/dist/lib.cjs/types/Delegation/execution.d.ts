import { Hex } from "viem";
import { BATCH_DEFAULT_MODE, BATCH_TRY_MODE, ExecutionMode, ExecutionStruct, REDEEM_DELEGATIONS_SELECTOR, SINGLE_DEFAULT_MODE, SINGLE_TRY_MODE } from "../utils/delegationTypes";
export type { ExecutionMode, ExecutionStruct };
export { BATCH_DEFAULT_MODE, BATCH_TRY_MODE, REDEEM_DELEGATIONS_SELECTOR, SINGLE_DEFAULT_MODE, SINGLE_TRY_MODE };
/**
 * Creates a normalized {@link ExecutionStruct} with checksummed `target`.
 */
export declare function createExecution(target?: Hex, value?: bigint, callData?: Hex): ExecutionStruct;
/**
 * Normalizes an execution by checksumming `target` while preserving `value` and `callData`.
 */
export declare function normalizeExecutionStruct(execution: ExecutionStruct): ExecutionStruct;
/**
 * ABI-packs a single execution as `(address, uint256, bytes)`.
 */
export declare function encodeSingleExecution(execution: ExecutionStruct): Hex;
/**
 * ABI-encodes multiple executions as `(address, uint256, bytes)[]`.
 */
export declare function encodeBatchExecution(executions: ExecutionStruct[]): Hex;
/**
 * Encodes one or more executions using single- or batch-packing based on length.
 */
export declare function encodeExecutionCalldata(executions: ExecutionStruct[]): Hex;
/**
 * Encodes each execution group in a batch independently.
 */
export declare function encodeExecutionCalldatas(executionsBatch: ExecutionStruct[][]): Hex[];
/**
 * Decodes ABI-packed single-execution calldata into an {@link ExecutionStruct}.
 */
export declare function decodeSingleExecution(data: Hex): ExecutionStruct;
/**
 * Decodes ABI-encoded batch execution calldata into {@link ExecutionStruct} entries.
 */
export declare function decodeBatchExecution(data: Hex): ExecutionStruct[];
/**
 * Returns whether the execution mode uses batch encoding (`BATCH_*` modes).
 */
export declare function isBatchExecutionMode(mode: ExecutionMode): boolean;
/**
 * Decodes execution calldata using the mode to choose single vs batch decoding.
 */
export declare function decodeExecutionCalldata(data: Hex, mode: ExecutionMode): ExecutionStruct[];
/**
 * Coerces arbitrary input into a valid `0x`-prefixed hex string for calldata fields.
 */
export declare function normalizeCallData(data: unknown): Hex;
/**
 * Throws if calldata does not start with the `redeemDelegations` function selector.
 */
export declare function assertRedeemDelegationsSelector(data: Hex): void;
/**
 * Strips the `redeemDelegations` selector and returns the ABI-encoded parameter bytes.
 */
export declare function stripRedeemDelegationsSelector(data: Hex): Hex;
/**
 * Prepends the `redeemDelegations` selector to ABI-encoded parameters.
 */
export declare function prependRedeemDelegationsSelector(encodedParams: Hex): Hex;
