import { Hex } from "viem";
import { Delegation, ExecutionMode, ExecutionStruct } from "../utils/delegationTypes";
import { BatchTransactionParams } from "../utils/eip5792Types";
/**
 * Converts EIP-5792 batch transaction params into delegation {@link ExecutionStruct} entries.
 */
export declare function batchTransactionParamsToExecutions(transactions: BatchTransactionParams[]): ExecutionStruct[];
/**
 * Converts delegation {@link ExecutionStruct} entries into EIP-5792 batch transaction params.
 */
export declare function executionsToBatchTransactionParams(executions: ExecutionStruct[]): BatchTransactionParams[];
/**
 * Decodes `redeemDelegations` calldata into delegations, execution modes, and executions.
 */
export declare function decodeRedeemDelegations(data: Hex): {
    delegations: Delegation[][];
    modes: ExecutionMode[];
    executions: ExecutionStruct[][];
};
