import { Hex } from "@toruslabs/metadata-helpers";
import { ApiRequestParams, IStorageManager } from "./interfaces";
export declare abstract class BaseStorageManager<T> implements IStorageManager<T> {
    protected _sessionId: Hex;
    get sessionId(): Hex;
    checkSessionParams(): void;
    /**
     * Common handler method for making an http request.
     *
     * Note: Embed all the query parameters in the path itself.
     */
    protected request<T>({ method, url, data, headers }: ApiRequestParams): Promise<T>;
    abstract setSessionId(sessionId: Hex): void;
    abstract createSession(data: T): Promise<string>;
    abstract authorizeSession(): Promise<T>;
    abstract updateSession(data: Partial<T>): Promise<void>;
    abstract invalidateSession(): Promise<boolean>;
}
