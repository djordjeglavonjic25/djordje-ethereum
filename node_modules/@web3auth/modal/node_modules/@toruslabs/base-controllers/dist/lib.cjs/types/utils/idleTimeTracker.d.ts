export declare const DEFAULT_IDLE_TIME: number;
type IdleTrackerEvents = {
    idle: () => void;
    active: () => void;
};
export declare class IdleTimeTracker {
    private readonly activityThresholdTime;
    private isIdle;
    private idleTimeout;
    private readonly emitter;
    private disposed;
    constructor(activityThresholdTime?: number);
    checkIfIdle(): boolean;
    cleanup(): void;
    dispose(): void;
    markIdle: () => void;
    markActive: () => void;
    on(event: keyof IdleTrackerEvents, listener: IdleTrackerEvents[keyof IdleTrackerEvents]): void;
    off(event: keyof IdleTrackerEvents, listener: IdleTrackerEvents[keyof IdleTrackerEvents]): void;
    private readonly resetTimerBound;
    private readonly disposeBound;
    private attach;
    private resetTimer;
    private clearTimer;
}
export declare const createIdleTimeTracker: (activityThresholdTime?: number) => IdleTimeTracker;
export declare const resetIdleTimeTrackerSingleton: () => void;
export {};
