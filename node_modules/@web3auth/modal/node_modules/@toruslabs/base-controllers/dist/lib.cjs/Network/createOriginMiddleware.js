'use strict';

function createOriginMiddleware(options) {
  return async ({
    context,
    next
  }) => {
    context.set("origin", options.origin);
    return next();
  };
}

exports.createOriginMiddleware = createOriginMiddleware;
