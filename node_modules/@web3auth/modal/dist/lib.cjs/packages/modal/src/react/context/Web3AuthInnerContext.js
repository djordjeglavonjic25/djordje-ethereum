'use strict';

var react = require('@web3auth/no-modal/react');
var react$1 = require('react');
var modalManager = require('../../modalManager.js');

function Web3AuthInnerProvider(params) {
  const {
    children,
    config,
    initialState
  } = params;
  const {
    web3AuthOptions
  } = config;
  const value = react.useWeb3AuthInnerContextValue({
    Web3AuthConstructor: modalManager.Web3Auth,
    web3AuthOptions,
    initialState,
    notReadyUsesCurrentStatus: true,
    cleanupOnUnmount: true,
    initEffectDependency: config
  });
  return /*#__PURE__*/react$1.createElement(react.Web3AuthInnerContext.Provider, {
    value
  }, children);
}

Object.defineProperty(exports, "Web3AuthInnerContext", {
    enumerable: true,
    get: function () { return react.Web3AuthInnerContext; }
});
exports.Web3AuthInnerProvider = Web3AuthInnerProvider;
