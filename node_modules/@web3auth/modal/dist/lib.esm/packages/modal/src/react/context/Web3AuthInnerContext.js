import { useWeb3AuthInnerContextValue, Web3AuthInnerContext } from '@web3auth/no-modal/react';
export { Web3AuthInnerContext } from '@web3auth/no-modal/react';
import { createElement } from 'react';
import { Web3Auth } from '../../modalManager.js';

function Web3AuthInnerProvider(params) {
  const {
    children,
    config,
    initialState
  } = params;
  const {
    web3AuthOptions
  } = config;
  const value = useWeb3AuthInnerContextValue({
    Web3AuthConstructor: Web3Auth,
    web3AuthOptions,
    initialState,
    notReadyUsesCurrentStatus: true,
    cleanupOnUnmount: true,
    initEffectDependency: config
  });
  return /*#__PURE__*/createElement(Web3AuthInnerContext.Provider, {
    value
  }, children);
}

export { Web3AuthInnerProvider };
