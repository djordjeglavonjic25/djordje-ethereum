'use strict';

var react = require('react');
var noModal = require('../../noModal.js');
var useWeb3AuthInnerContextValue = require('./useWeb3AuthInnerContextValue.js');

const Web3AuthInnerContext = react.createContext(null);
function Web3AuthInnerProvider(params) {
  const {
    children,
    config,
    initialState
  } = params;
  const {
    web3AuthOptions
  } = config;
  const value = useWeb3AuthInnerContextValue.useWeb3AuthInnerContextValue({
    Web3AuthConstructor: noModal.Web3AuthNoModal,
    web3AuthOptions,
    initialState
  });
  return react.createElement(Web3AuthInnerContext.Provider, {
    value
  }, children);
}

exports.Web3AuthInnerContext = Web3AuthInnerContext;
exports.Web3AuthInnerProvider = Web3AuthInnerProvider;
