import { type SolanaSignAndSendTransactionFeature, type SolanaSignMessageFeature, type SolanaSignTransactionFeature } from "@solana/wallet-standard-features";
import { type Wallet, type WalletWithFeatures } from "@wallet-standard/base";
import { type StandardConnectFeature, type StandardDisconnectFeature, type StandardEventsFeature } from "@wallet-standard/features";
type WalletStandard = WalletWithFeatures<StandardConnectFeature & StandardDisconnectFeature & StandardEventsFeature & SolanaSignAndSendTransactionFeature & SolanaSignMessageFeature & SolanaSignTransactionFeature>;
import { BaseConnectorLoginParams, BaseConnectorSettings, ChainNamespaceType, Connection, CONNECTOR_CATEGORY_TYPE, CONNECTOR_STATUS_TYPE, ConnectorFn, ConnectorInitOptions, ConnectorNamespaceType, UserInfo } from "../../base";
import { BaseSolanaConnector } from "../base-solana-connector";
export declare class WalletStandardConnector extends BaseSolanaConnector<void> {
    readonly name: string;
    readonly connectorNamespace: ConnectorNamespaceType;
    readonly currentChainNamespace: ChainNamespaceType;
    readonly type: CONNECTOR_CATEGORY_TYPE;
    readonly isInjected = true;
    status: CONNECTOR_STATUS_TYPE;
    private wallet;
    constructor(options: BaseConnectorSettings & {
        name: string;
        wallet: Wallet;
    });
    get solanaWallet(): WalletStandard | null;
    get isWalletConnected(): boolean;
    init(options: ConnectorInitOptions): Promise<void>;
    connect({ chainId, getAuthTokenInfo }: BaseConnectorLoginParams): Promise<Connection | null>;
    disconnect(options?: {
        cleanup: boolean;
    }): Promise<void>;
    getUserInfo(): Promise<Partial<UserInfo>>;
    switchChain(_params: {
        chainId: string;
    }, _init?: boolean): Promise<void>;
    enableMFA(): Promise<void>;
    manageMFA(): Promise<void>;
}
export declare const walletStandardConnector: (wallet: Wallet) => ConnectorFn;
export {};
