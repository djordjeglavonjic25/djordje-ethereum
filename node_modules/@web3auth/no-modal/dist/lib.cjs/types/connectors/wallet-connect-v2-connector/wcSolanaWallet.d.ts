import { type SolanaSignAndSendTransactionFeature, type SolanaSignMessageFeature, type SolanaSignTransactionFeature } from "@solana/wallet-standard-features";
import { type IdentifierArray, type Wallet, type WalletAccount, type WalletIcon, type WalletVersion } from "@wallet-standard/base";
import { type StandardConnectFeature, type StandardDisconnectFeature, type StandardEventsFeature } from "@wallet-standard/features";
import type { ISignClient } from "@walletconnect/types";
import { type CustomChainConfig } from "../../base";
type WCSolanaFeatures = StandardConnectFeature & StandardDisconnectFeature & StandardEventsFeature & SolanaSignAndSendTransactionFeature & SolanaSignMessageFeature & SolanaSignTransactionFeature;
/**
 * WCSolanaWallet implements the Wallet Standard interface by routing directly to
 * a WalletConnect SignClient — no intermediate JRPC engine needed.
 */
export declare class WCSolanaWallet implements Wallet {
    readonly version: WalletVersion;
    readonly name = "WalletConnect";
    readonly icon: WalletIcon;
    readonly chains: IdentifierArray;
    readonly features: WCSolanaFeatures;
    private _accounts;
    private readonly _listeners;
    private constructor();
    get accounts(): readonly WalletAccount[];
    /**
     * Creates a WCSolanaWallet by reading accounts from the active WC session.
     */
    static create(connector: ISignClient, chainConfig: CustomChainConfig): Promise<WCSolanaWallet | null>;
}
export {};
