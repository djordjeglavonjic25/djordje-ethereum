import { type JRPCMiddlewareV2, type JRPCRequest, type Json } from "@web3auth/auth";
import { type IdleTimeTracker } from "../utils";
export declare function createIdleTimeTrackerMiddleware({ idleTimeTracker, isSessionActive, }: {
    idleTimeTracker: IdleTimeTracker;
    isSessionActive?: () => boolean;
}): JRPCMiddlewareV2<JRPCRequest, Json>;
