import { type WEB3AUTH_NETWORK_TYPE } from "@web3auth/auth";
import { ChainNamespaceType } from "../Network/INetworkController";
export interface DeviceInfo {
    platform: string;
    os: string;
    osVersion: string;
    browser: string;
    browserVersion: string;
}
export interface SiwwTokens {
    idToken: string;
    accessToken: string;
    refreshToken: string;
}
export interface VerifySignedChallengeParams {
    chainNamespace: ChainNamespaceType;
    signedMessage: string;
    challenge: string;
    connector: string;
    authServer: string;
    web3AuthClientId: string;
    web3AuthNetwork: WEB3AUTH_NETWORK_TYPE;
    sessionTimeout: number;
    deviceInfo: DeviceInfo;
    sessionNamespace?: string;
}
export interface SiwwPayload {
    domain: string;
    address: string;
    uri: string;
    version: string;
    chainId: number | string;
    nonce: string;
    issuedAt: string;
    scheme?: string;
    statement?: string;
    expirationTime?: string;
    notBefore?: string;
    requestId?: string;
    resources?: string[];
}
export declare function getDeviceInfo(): DeviceInfo;
export declare const signChallenge: (payload: SiwwPayload, chainNamespace: ChainNamespaceType, authServer: string) => Promise<string>;
export declare const verifySignedChallenge: (params: VerifySignedChallengeParams) => Promise<SiwwTokens>;
