import type { DuplexOptions } from "readable-stream";
import { BasePostMessageStream } from "./basePostMessageStream";
export interface PostMessageStreamArgs extends DuplexOptions {
    name: string;
    target: string;
    targetOrigin?: string;
    targetWindow?: Window;
}
export declare class PostMessageStream extends BasePostMessageStream {
    private _name;
    private _target;
    private _targetOrigin;
    private _targetWindow;
    private _onMessage;
    constructor({ name, target, targetOrigin, targetWindow, ...streamOptions }: PostMessageStreamArgs);
    _destroy(): void;
    protected _postMessage(data: unknown): void;
    private _onMessageHandler;
}
