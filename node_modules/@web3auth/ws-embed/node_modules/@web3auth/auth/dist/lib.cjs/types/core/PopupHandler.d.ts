import { SecurePubSub } from "@toruslabs/secure-pub-sub";
import type { default as TypedEmitter } from "typed-emitter";
import { type AuthFlowResult } from "../utils";
export type PopupHandlerEvents = {
    close: () => void;
};
declare const PopupHandler_base: new () => TypedEmitter<PopupHandlerEvents>;
declare class PopupHandler extends PopupHandler_base {
    url: string;
    target: string;
    features: string;
    window: Window;
    windowTimer: number;
    iClosedWindow: boolean;
    timeout: number;
    socketUrl: string;
    serverUrl: string;
    securePubSub: SecurePubSub | null;
    constructor({ url, target, features, timeout, socketUrl, serverUrl, }: {
        url: string;
        socketUrl: string;
        serverUrl: string;
        target?: string;
        features?: string;
        timeout?: number;
    });
    _setupTimer(): void;
    open(): void;
    close(): void;
    redirect(locationReplaceOnRedirect: boolean): void;
    listenOnChannel(loginId: string): Promise<AuthFlowResult | null>;
}
export default PopupHandler;
