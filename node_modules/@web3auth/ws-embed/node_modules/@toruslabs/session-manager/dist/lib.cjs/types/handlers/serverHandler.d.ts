import { Hex } from "@toruslabs/metadata-helpers";
import type { ApiRequestParams, StorageHandler, StorageHandlerRetrieveOptions, StorageHandlerStoreOptions } from "../interfaces";
type RequestFn = <T>(params: ApiRequestParams) => Promise<T>;
export declare class ServerHandler<T> implements StorageHandler<T> {
    private readonly sessionServerBaseUrl;
    private readonly request;
    constructor(sessionServerBaseUrl: string, request: RequestFn);
    /**
     * Store data on the server.
     * @param keyHex - The private key to use for encryption in hex format.
     * @param data - The data to store.
     * @param options - The options for the request.
     * @returns void
     */
    storeData(keyHex: Hex, data: Partial<T> | T | Record<string, never>, options?: StorageHandlerStoreOptions): Promise<void>;
    /**
     * Retrieve data from the server.
     * @param keyHex - The public key to use for decryption in hex format.
     * @param options - The options for the request.
     * @returns The data retrieved from the server.
     */
    retrieveData(keyHex: Hex, options?: StorageHandlerRetrieveOptions): Promise<T | undefined>;
    clearStorage(_key: Hex): void;
    clearOrphanedData(_baseKey?: string): void;
    getStorageKey(keyHex: Hex): string;
}
export {};
