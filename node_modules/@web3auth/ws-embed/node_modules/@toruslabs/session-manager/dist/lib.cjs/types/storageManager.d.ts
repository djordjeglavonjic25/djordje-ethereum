import { Hex } from "@toruslabs/metadata-helpers";
import { BaseStorageManager } from "./base";
import { type StorageManagerOptions } from "./interfaces";
export declare class StorageManager<T> extends BaseStorageManager<T> {
    sessionServerBaseUrl: string;
    sessionNamespace: string;
    allowedOrigin: string | boolean;
    sessionTime: number;
    /**
     * The session id to use for storage. Must be a 32 byte hex string.
     */
    protected _sessionId: Hex;
    private serverHandler;
    private readonly useLocalStorage;
    private localStorageHandler?;
    constructor({ sessionServerBaseUrl, sessionNamespace, sessionTime, sessionId, allowedOrigin, useLocalStorage }: StorageManagerOptions);
    get localStorageHandlerStorageKey(): string | undefined;
    get serverHandlerStorageKey(): string;
    private get baseLocalStorageKey();
    static generateRandomSessionKey(): Hex;
    /**
     * Sets the session id.
     * @param sessionId - The session id to set. Must be a 64 character hex string.
     */
    setSessionId(sessionId: Hex): void;
    createSession(data: T, headers?: RequestInit["headers"]): Promise<string>;
    authorizeSession({ headers }?: {
        headers: RequestInit["headers"];
    }): Promise<T>;
    updateSession(data: Partial<T>, headers?: RequestInit["headers"]): Promise<void>;
    invalidateSession(headers?: RequestInit["headers"]): Promise<boolean>;
    clearStorage(): void;
    clearOrphanedData(baseKey?: string): void;
    /**
     * Runs a local-storage operation, swallowing any errors so that failures
     * like QuotaExceededError never break the primary server-backed flow.
     */
    private safeLocalStorageOp;
    private getLocalStorageHandler;
}
