function createIdleTimeTrackerMiddleware({
  idleTimeTracker,
  isSessionActive
}) {
  return async ({
    next
  }) => {
    var _isSessionActive;
    const sessionActive = (_isSessionActive = isSessionActive === null || isSessionActive === void 0 ? void 0 : isSessionActive()) !== null && _isSessionActive !== void 0 ? _isSessionActive : true;
    if (sessionActive && idleTimeTracker.checkIfIdle()) {
      idleTimeTracker.markActive();
    }
    return next();
  };
}

export { createIdleTimeTrackerMiddleware };
